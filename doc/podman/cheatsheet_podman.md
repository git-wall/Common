```shell
 podman machine list
```

```shell
 podman machine start
```

```shell
 podman machine stop
```

```shell
 podman machine ssh
```

```shell
 podman machine init --cpus 4 --memory 8192 --disk-size 50
```

```shell
 podman machine rm
```

```shell
 podman machine inspect
```

```shell
  podman logs <container_id|container_name>
```

```shell
  podman logs --tail 20 <container_id|container_name>
```